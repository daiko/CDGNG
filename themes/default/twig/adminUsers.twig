<table class='table table-striped table-condensed table-hover'>
  <tr>
    <th class='col-md-1'>Identifiant</th>
    <th class='col-md-4'>Mot de passe</th>
    <th class='col-md-1'></th>
  </tr>
  <tr>
    <form action="?view=adminUsers&action=addUser" method="post">
      <td><input type='text' name='login' pattern='[A-Za-z]+' placeholder='Identifiant' required /></td>
      <td><input type='password' name='password1' placeholder='Mot de passe' required />
      <input type='password' name='password2' placeholder='Vérification MdP' /></td>
      <td class='text-right'><button class='btn btn-success' type='submit'><span class="glyphicon glyphicon-plus" aria-hidden="true"></button></td>
    </form>


  {% for user in users %}
    <tr>
      <td><b>{{ user.login }}</b></td>
      <td></td>
      <td class='text-right'>
        <a class='btn btn-xs btn-danger' href="?view=adminUsers&action=delUser&login={{ user.login }}">
          <span class="glyphicon glyphicon-trash" aria-hidden="true">
        </a>
      </td>
    </tr>
  {% endfor %}
</table>
